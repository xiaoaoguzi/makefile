  Makefile 由一条条的规则组成
# 每一条规则的格式如下:
#
# 目标文件: 依赖文件列表
#       需要执行的命令
#
# 每行命令必须以tab打头
# 依赖关系和命令 都可已有零个到多个
#
# make的用法:  make 目标:
# make自动查找该目录下的Makefile文件中是否有匹配目标的规则
# 找到的话 判断该规则的依赖文件是否比目标文件要新 是的话 执行该>规则的命令
#
# Makefile中常用的自动变量类型:
#
# $@    表示该规则中的目标文件
# $^    表示该规则中的所有依赖文件
# $<    表示该规则中的第一个依赖文件

